specVersion: "v1"
serviceId: "fireapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fireapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
